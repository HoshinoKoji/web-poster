{
  "name": "web-poster",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "check:static": "bun scripts/check-static-build.ts",
    "build": "tsc --noEmit && vite build && bun run check:static",
    "preview": "vite preview",
    "pdf": "bun run build && vivliostyle build",
    "pdf:preview": "bun run build && vivliostyle preview"
  },
  "devDependencies": {
    "@vivliostyle/cli": "^11.1.0",
    "typescript": "^7.0.2",
    "vite": "^8.1.5"
  }
}
